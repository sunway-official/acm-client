query getAgenda($topics: [ID!]!) {
  getAgenda(topics: $topics) {
    id
    start
    end
    activity_id
    activity_title
    activity_description
    room_name
    topics {
      topic_id
      topic_name
    }
    personalSchedule {
      id
    }
  }
}
